class DBModel {
  int? id, status;
  String? title, notes, amount, category, date, time;

  DBModel({
    this.id,
    this.status,
    this.title,
    this.notes,
    this.amount,
    this.category,
    this.date,
    this.time,
  });
}
